<% post.post_comments.each do |comment| %>
  <%= comment.created_at.strftime('%Y/%m/%d') %><%= comment.comment %>
  <%= link_to post_post_comment_path(comment.post, comment), method: :delete, remote: true do %>
    <i class="fa-regular fa-trash-can"></i>
  <% end %>
<% end %>
